# Testes

Comandos principais:

```powershell
.\gradlew.bat testDebugUnitTest
.\gradlew.bat lintDebug
.\gradlew.bat assembleDebug
.\gradlew.bat connectedDebugAndroidTest
python tools/audit_pt_pt.py
```

Os testes unitários validam catálogo de cartas, raridades, compras, duplicados de baús, migração e sons S/Z/X/J.
